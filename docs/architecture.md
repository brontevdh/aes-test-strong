# Architecture

This repo is a scanner target. It contains no runtime code. Its structure exists only to exercise every branch of the AES readiness scanner: governance controls, agent instructions, docs, templates.