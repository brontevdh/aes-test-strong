# Copilot instructions

This repository is a test target for the AES readiness advisor. When making changes:

- Prefer small, focused pull requests.
- Update docs alongside behavior changes.
- Every PR must pass the `lint` check in `.github/workflows/ci.yml`.
- Route review through the owner listed in CODEOWNERS.